{
  "properties": {
    "user.name": {
      "description": "The authenticated user.",
      "type": "string"
    },

    "buffersize": {
      "description": "The size of the buffer used in transferring data.",
      "type": "integer"
    }
  }
}